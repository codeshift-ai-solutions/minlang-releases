# minlang-releases

Public distribution mirror for the `ml1` MinLang compiler:
prebuilt binaries (Releases), install scripts (`install/`), and the
`setup-ml1` / `minlang-compile` GitHub Actions
(`.github/actions/`). Source lives in the private
`minlang-core` repository. License for distributed runtime
packages: MIT.
